apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-node-app
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-node-app
  template:
    metadata:
      labels:
        app: simple-node-app
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: simple-node-app
        image: node:alpine
        command: ["/bin/sh", "-c"]
        args:
        - "apk add --no-cache nodejs && mkdir -p /tmp/app && cp /app/app.js /tmp/app/ && cd /tmp/app && node app.js"
        ports:
        - containerPort: 3000
        env:
        - name: PORT
          value: "3000"
        volumeMounts:
        - name: app-code
          mountPath: /app
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: app-code
        configMap:
          name: simple-node-app-code
---
apiVersion: v1
kind: Service
metadata:
  name: simple-node-app-service
  namespace: microservices
  labels:
    app: simple-node-app
spec:
  selector:
    app: simple-node-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
